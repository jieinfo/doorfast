#!/bin/sh

# Use OpenWrt's account helpers; BusyBox does not expose addgroup on all images.
. /lib/functions.sh
if ! group_exists doorfast; then
	group_add_next doorfast >/dev/null
	group_exists doorfast || exit 1
fi

exit 0
