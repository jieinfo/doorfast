# 贡献与协议证据规范

Doorfast 通过可复现证据增加设备兼容性。提交前请移除住户身份、设备密码、令牌、
公网地址、视频内容和任何可识别个人的信息。

## 可以贡献什么

每个品牌、型号与固件组合可提交以下任一材料：

- 脱敏 SIP 或控制数据包夹具，以及采集条件。
- 事件名称、字段含义和设备可见结果的对应关系。
- 一次控制操作的请求、响应、设备结果和失败结果。
- 设备型号、固件版本、网络拓扑摘要与测试日期。

请用占位符替换敏感字段，例如 `<INDOOR_ID>`、`<DOOR_IP>`、`<TOKEN>`；不要只删除
字段，否则解析规则无法验证其位置和编码。

## 控制驱动的准入门槛

一个开门、挂断、召梯或楼层开放操作必须同时满足：

1. 请求和成功响应均有脱敏夹具。
2. 夹具能通过自动回放测试，并明确字段来源。
3. 贡献者确认该样本来自其有权测试的设备，且记录了实际结果。
4. 至少有一个失败或拒绝场景，证明实现不会把异常响应当作成功。
5. 驱动默认关闭，且只对已批准的目标端点发送。

在缺少其中任一项时，功能只能作为被动识别或模拟决策存在，不能发送网络控制报文。

## 提交格式

新增夹具时，请同时提供一个 Markdown 说明，包含：品牌、型号、固件、操作类型、
采集方向、脱敏规则、预期归一化事件或预期响应，以及已知限制。不要提交私钥、
激活码、完整配置备份或原始视频。
